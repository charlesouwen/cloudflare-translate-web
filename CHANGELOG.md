# 变更日志

本项目按源码目标版本记录用户可见变更。生产环境版本以 `/api/status` 为准。

## translate-v29 - 2026-09-03

### 新增

- 新增会议双通道：分别采集对方共享音频与我方物理麦克风，并固定双方识别/翻译方向。
- 新增麦克风输入设备与 TTS 输出设备选择；兼容浏览器可通过 `setSinkId()` 将译音定向到 VB-CABLE 或 VoiceMeeter。
- 新增可选 STT 声学证据 Header：`X-Audio-Voiced-Ms` 与 `X-Audio-Peak`。旧客户端无需发送这些字段。
- 新增 Teams Web、Teams 桌面版和虚拟音频路由文档。

### 修复

- 恢复学习模式的独立 `/api/learn` 调用、加载状态、结果展示和失败降级；学习请求不再影响基础翻译。
- 修复共享系统音频自动切换到音乐模式、进而绕过对话 VAD 与严格幻觉过滤的问题。
- 加强静音、低声学证据、固定字幕、点赞/关注/订阅模板、重复内容和脚本冲突过滤，避免无真实语音时产生译文。
- 阻止被过滤的识别结果进入翻译、TTS 或后续识别上下文。

### 改进

- 会议模式只允许我方译文自动播报；对方译文默认仅显示。
- TTS 播放期间暂停冲突音频通道，播放结束后保留恢复保护窗，并用近期 TTS 文本指纹抑制回声重识别。
- 优化 Fusion 同传的实时稿与最终稿协作、音量/VAD 门控、最终识别排队和重复结果合并。
- 补充音频权限、设备掉线、无共享音轨与浏览器能力降级的界面状态。

### 兼容性

- 保留现有 API 路径、原必填字段、请求别名和既有返回字段语义。
- 新增 STT Header 和返回附加信息均为可选扩展；不发送新字段的小程序继续沿用原处理流程。
